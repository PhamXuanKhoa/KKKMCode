services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "6969:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "3618:3000"
    environment:
      - NODE_ENV=production
